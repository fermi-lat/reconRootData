#====================================================
${CONSTITUENT}:: ${CINTFILE}
	@echo "------> ${CONSTITUENT} ok"

VPATH=../reconRootData/
${CINTFILE}:: ${root_headers}
	@echo " running rootcint to generate ${CINTFILE} "
	cd ../${package}/ ; ${ROOT_PATH}/bin/rootcint -f ${CINTFILE} -c -I${DIGIROOTDATAROOT} ${root_headers}
#==================================================== 
